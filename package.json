{
  "private": true,
  "name": "danharris-io-landing-v3",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && serve -s ./build",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^4.5.0",
    "eslint-config-synacor": "^1.1.0",
    "if-env": "^1.0.0",
    "path": "^0.12.7",
    "preact-cli": "^2.0.0",
    "react-snap": "^1.20.0"
  },
  "dependencies": {
    "emotion": "^9.2.12",
    "preact": "^8.2.1",
    "preact-compat": "^3.17.0",
    "preact-emotion": "^9.2.12",
    "window-or-global": "^1.0.1"
  }
}
